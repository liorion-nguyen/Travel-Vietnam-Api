export enum DiscountStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  EXPIRED = 'EXPIRED',
  PENDING = 'PENDING',
}

export enum DiscountType {
  TOUR = 'TOUR',
  HOTEL = 'HOTEL',
  ALL = 'ALL',
}
